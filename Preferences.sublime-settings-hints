// This file contains information about some hidden settings
// which are used by PackageDev to provide completions.
{
    // Controls whether to clear selections, if anything is selected
    // in another window or application.
    "clear_selection": false,

    // Renders text in a tokenization debug mode. It can be used for
    // syntax development to make sure all words are tokenized as
    // expected.
    "draw_debug": false,

    // Renders autocompletion suggestions as phantom directly after
    // the caret instead of popping up the completion panel.
    "mini_auto_complete": false,

    // Controls whether to display an automatically generated name
    // on a view's tab control based on its content.
    // If `false` tabs of unsaved files always display "untitled".
    "set_unsaved_view_name": true,
}
